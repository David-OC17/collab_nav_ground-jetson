[develop]
script_dir=$base/lib/mission_orchestrator
[install]
install_scripts=$base/lib/mission_orchestrator

[tool:pytest]
testpaths = test
pythonpath = test
markers =
    integration: requires full workspace + hardware (oradar, ffmpeg). Run with -m integration.
addopts = -m "not integration"
